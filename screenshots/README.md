# Screenshots

- `banner.svg` — the hero banner used at the top of the README.
- `dashboard-preview.svg` — a **design preview** of the dashboard layout (glassmorphism cards, stat tiles, charts, reminders, timeline), used in the README to illustrate the UI.

> **Note:** these two files are illustrative design graphics, not literal screen captures of a running instance — this project was generated as source code, not executed in a browser. After you run the app locally (`docker compose up` or `mvn spring-boot:run`), it's a good idea to replace these with real screenshots of:
>
> 1. The login page
> 2. The dashboard
> 3. The pets grid
> 4. A pet detail page
> 5. The appointments timeline
> 6. The expenses analytics view
>
> Drop your PNGs in this folder using the same file names referenced in `README.md` (or update the links) and they'll appear automatically on GitHub.
